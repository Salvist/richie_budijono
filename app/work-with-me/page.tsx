import type { Metadata } from "next";
import { CTASection, PageHero, SectionHeader } from "../components/ui";
import { process, services } from "../lib/data";

export const metadata: Metadata={title:"Work With Me",description:"Mobile, web, MVP, Firebase, and AI product development with Richie Budijono."};
export default function WorkPage(){return <><PageHero eyebrow="Work with me" title={<>A practical partner for products that need to <em>move.</em></>} intro="Whether you have a rough concept or a product already in motion, I can help turn the next important piece into dependable software."/><div className="services-page container"><div className="services-grid">{services.map(s=><article className="service-card" key={s.number}><span className="service-number">{s.number}</span><h3>{s.title}</h3><p>{s.description}</p><ul>{s.deliverables.map(i=><li key={i}>{i}</li>)}</ul></article>)}</div>
  <section className="engagement-block"><div><p className="eyebrow">A good fit</p></div><div className="engagement-copy"><h2>Best when the problem matters more than the buzzwords.</h2><p>I’m most useful when you need someone who can reason about the product and own the implementation—not just execute a disconnected list of tasks.</p><div className="ideal-list"><p><span>01</span>You need to shape and ship a focused MVP.</p><p><span>02</span>Your mobile or web product needs an experienced build partner.</p><p><span>03</span>You need Firebase, API, or AI capability integrated thoughtfully.</p><p><span>04</span>You value clear communication and practical technical decisions.</p></div></div></section>
  <section className="section"><SectionHeader eyebrow="How we get there" title="A clear path from idea to launch."/><ol className="process-list light-process">{process.map(([title,copy],i)=><li key={title}><span>0{i+1}</span><h3>{title}</h3><p>{copy}</p></li>)}</ol></section></div><CTASection/></>}
